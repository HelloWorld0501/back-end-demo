spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: demo-consumer
    config:
      discovery:
        enabled: true
        service-id: demo-config
      name: demo-consumer
      profile: pro
  rabbitmq:
    host: localhost
    port: 5672
    username: 你的用户名
    password: 你的密码
#此文件加载优先级高于application.yml